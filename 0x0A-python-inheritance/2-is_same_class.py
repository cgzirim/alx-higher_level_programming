#!/usr/bin/python3
"""Defines a function that compares an instance and a class."""


def is_same_class(obj, a_class):
    """Compares an instance and a class."""
    return (isinstance(obj, a_class))
